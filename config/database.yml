DEFAULTS: &DEFAULTS
    adapter: mysql2
    encoding: utf8
    database: test
    username: root
    password: mysql
    host: localhost
    port: 3306

development:
    <<: *DEFAULTS

test:
    <<: *DEFAULTS

production:
    <<: *DEFAULTS
