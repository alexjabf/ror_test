/ Projects Row
.row
  .col-lg-12
    %h1.page-header
      Detalles de Tus Contactos
      %small Aquí puedes ver y editar tus contactos o dar de baja alguno.
    %ol.breadcrumb
      %li
        %a{:href => "/"} Inicio
      %li.active= link_to 'Crear Contacto', new_user_contact_path, title: 'Crea un nuevo contacto.'
.panel.panel-primary.col-lg-12
  =render 'partials/admin/records_per_page'
  %table.table
    %thead
      %tr
        %th Id
        - if is_admin?
          %th Su Contacto
        %th Nombre
        %th Email
        %th Telefono
        %th Direccion

    %tbody
      - @user_contacts.each do |user_contact|
        - @model = user_contact
        %tr
          %td= custom_link(controller_name, 'show', user_contact.id, 'btn btn-xs btn-primary')
          - if is_admin?
            %td= custom_link('user', 'show', user_contact.user, nil)
          %td= user_contact.name
          %td= user_contact.email
          %td= user_contact.phone
          %td= user_contact.address
          %td= render 'partials/admin/index_links'